# Copy this file to .env and fill values for local development.
# Do NOT commit real secrets to git.

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/laundrydb

# Public
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE=/api

# NextAuth / token secrets (example, keep secure in production)
NEXT_AUTH_SECRET=replace-with-a-strong-secret
NEXT_AUTH_SECRET=replace-with-a-strong-secret
NEXT_INTERNAL_API_SECRET=replace-with-a-strong-secret

# OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret